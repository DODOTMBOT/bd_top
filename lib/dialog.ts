'use client'
import { useDialog } from '@/components/ui/DialogCenter'

// Хук для компонентов
export const useAppDialog = useDialog

// Фолбэк для не-React кода (по месту импортируй и передавай функции из контекста при необходимости)
